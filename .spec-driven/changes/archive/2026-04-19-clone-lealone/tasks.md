# Tasks: clone-lealone

## Implementation
- [x] Clone the Lealone repository from GitHub into `lealone-source/` at the project root
- [x] Verify the cloned repository contains expected top-level files (`pom.xml`, `README.md`)

## Testing
- [x] Run `ls lealone-source/pom.xml` — validate the source tree is present
- [x] Run `bash -c 'cd lealone-source && git log --oneline -1'` — unit test that the repo has valid git history

## Verification
- [x] Verify implementation matches proposal scope
